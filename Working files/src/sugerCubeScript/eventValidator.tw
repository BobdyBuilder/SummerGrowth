:: eventValidator {"position":"225,50","size":"100,100"}
<<script>>
(() => {
	const events = setup.events;
	const firstSeenAt = new Map();
	const duplicateNames = [];
	const emptyContentNames = [];
	const directMutationNames = [];
	// Tracked body-size stats now have a single entrypoint (widget/addBodyStat.tw,
	// and the addMuscleSlow/addFatSlow/addBreastsSlow/addHeightSlow/addDickSlow
	// wrappers around it) so growth multipliers and clamps live in one place.
	// Flag any event that still mutates one of these directly instead.
	const directMutationRe = /\$(muscle|fat|breasts|height|dick)(?![a-zA-Z])\s*(\+=|-=|\*=|\/=|=(?!=)|to\s)/;

	for (let i = 0; i < events.length; i++) {
		const event = events[i];

		if (firstSeenAt.has(event.name)) {
			duplicateNames.push({ name: event.name, indexes: [firstSeenAt.get(event.name), i] });
		} else {
			firstSeenAt.set(event.name, i);
		}

		if (event.content === "") {
			emptyContentNames.push(event.name);
		}

		if (typeof event.content === "string" && directMutationRe.test(event.content)) {
			directMutationNames.push(event.name);
		}
	}

	if (duplicateNames.length > 0) {
		console.warn("eventValidator: duplicate event name(s) found - only one definition is ever reachable per name:");
		duplicateNames.forEach(dup => {
			console.warn(`  "${dup.name}" defined at setup.events[${dup.indexes[0]}] and setup.events[${dup.indexes[1]}]`);
		});
	}

	if (emptyContentNames.length > 0) {
		console.log(`eventValidator: ${emptyContentNames.length} event(s) have empty content (stubs): ${emptyContentNames.join(", ")}`);
	}

	if (directMutationNames.length > 0) {
		console.warn(`eventValidator: ${directMutationNames.length} event(s) mutate a tracked body stat directly instead of through <<addBodyStat>>/addXSlow: ${directMutationNames.join(", ")}`);
	}
})();
<</script>>


